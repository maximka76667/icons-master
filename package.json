{
  "name": "@maximka76667/icons-master",
  "version": "1.0.1",
  "description": "CLI tool to manage lucide-react icon exports",
  "main": "bin/index.js",
  "bin": {
    "icons-master": "bin/index.js"
  },
  "files": [
    "bin/"
  ],
  "scripts": {
    "build": "cargo build --release",
    "copy-bin": "if not exist bin mkdir bin && copy /y target\\release\\icons-master.exe bin\\",
    "prepublishOnly": "npm run build && npm run copy-bin"
  }
}
